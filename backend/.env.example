PORT=5000

# MongoDB connection string (replace <username> and <password>)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/skilllink

# Secret for JWT tokens
JWT_SECRET=your_jwt_secret_here

# Directory for file uploads (if used in your app)
UPLOAD_DIR=backend/uploads

# Optional: Set this to true if you use it anywhere
NODE_ENV=development